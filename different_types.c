#include <stdio.h>
#include <stdlib.h>

int main()
{
    size_t rows = 10, columns = 10;
    printf("Enter rows & columns : ");
    ///scanf("%d %d", &rows, &columns);

    int arr[rows][columns];

    return 0;
}